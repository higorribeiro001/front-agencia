import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["127.0.0.1", "amused-workable-anteater.ngrok-free.app", "https://agencia-api-rest.onrender.com"],
  }
};

export default nextConfig;
